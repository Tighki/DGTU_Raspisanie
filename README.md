# DGTY Timetable Telegram Bot - Python Version

Telegram-бот для просмотра расписания занятий студентов и преподавателей ДГТУ и ПИ ДГТУ (Python версия).

## Возможности

- 🔐 Авторизация пользователей (студенты и преподаватели)
- 📅 Просмотр расписания на сегодня, завтра и неделю
- 🏫 Поддержка двух университетов: ДГТУ и ПИ ДГТУ
- 💾 Хранилище данных на MongoDB

## Требования

- Python 3.8+
- Токен Telegram-бота (получить у [@BotFather](https://t.me/BotFather))
- MongoDB (локально или удаленно)

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Установите переменные окружения:
```env
BOT_TOKEN=ваш_токен_бота
MONGO_URI=mongodb://localhost:27017/
MONGO_DB=ras
MONGO_COLLECTION=sessions
```

3. Запустите бота:
```bash
python main.py
```

## Переменные окружения

| Переменная | Описание | Обязательна | По умолчанию |
|------------|----------|-------------|--------------|
| `BOT_TOKEN` | Токен Telegram-бота от @BotFather | Да | - |
| `MONGO_URI` | URI подключения к MongoDB | Нет | `mongodb://mongo:3zzc8t1bephxkkvn@192.168.1.182:27019/` |
| `MONGO_DB` | Название базы данных | Нет | `ras` |
| `MONGO_COLLECTION` | Название коллекции | Нет | `sessions` |

## Структура проекта

```
python_bot/
├── main.py              # Точка входа
├── bot/                 # Основной пакет бота
│   ├── __init__.py
│   ├── bot.py           # Основной класс бота
│   ├── handlers.py      # Обработчики команд
│   ├── config.py        # Конфигурация
│   ├── constants.py     # Константы
│   ├── utils.py         # Утилиты
│   ├── localizer.py     # Локализация
│   ├── menu.py          # Меню и кнопки
│   └── api/             # API клиенты
│       ├── __init__.py
│       └── timetable.py # API расписания
├── requirements.txt     # Зависимости
├── Dockerfile          # Docker конфигурация
└── README.md          # Документация
```

## Команды

- `/start` - Запустить бота
- `/l` или `/login` - Начать процесс авторизации

## Деплой

### Docker

```bash
docker build -t dgty-bot .
docker run -e BOT_TOKEN=ваш_токен -e MONGO_URI=ваш_uri dgty-bot
```

## Лицензия

MIT License
