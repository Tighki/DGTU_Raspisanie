# DGTY Timetable Telegram Bot - Python Version

Telegram-бот для просмотра расписания занятий студентов и преподавателей ДГТУ и ПИ ДГТУ (Python версия).

## Возможности

- 🔐 Авторизация пользователей (студенты и преподаватели)
- 📅 Просмотр расписания на сегодня, завтра и неделю
- 🏫 Поддержка двух университетов: ДГТУ и ПИ ДГТУ
- 💾 Гибкое хранилище данных (memory, SQLite, Redis)

## Требования

- Python 3.8+
- Токен Telegram-бота (получить у [@BotFather](https://t.me/BotFather))

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` из примера:
```bash
cp .env.example .env
```

3. Отредактируйте `.env` и добавьте токен бота:
```env
BOT_TOKEN=ваш_токен_бота
STORAGE_TYPE=sqlite
SQLITE_PATH=sessions.db
```

4. Запустите бота:
```bash
python main.py
```

## Переменные окружения

| Переменная | Описание | Обязательна | По умолчанию |
|------------|----------|-------------|--------------|
| `BOT_TOKEN` | Токен Telegram-бота от @BotFather | Да | - |
| `STORAGE_TYPE` | Тип хранилища: `memory`, `sqlite`, `redis` | Нет | `memory` |
| `SQLITE_PATH` | Путь к файлу SQLite (для `sqlite`) | Нет | `sessions.db` |
| `REDIS_HOST` | Хост Redis (для `redis`) | Да (для redis) | - |
| `REDIS_PORT` | Порт Redis (для `redis`) | Нет | `6379` |

## Структура проекта

```
python_bot/
├── main.py              # Точка входа
├── bot.py               # Основной класс бота
├── config.py            # Конфигурация
├── handlers.py          # Обработчики команд
├── storage.py           # Хранилище данных
├── timetable_api.py     # Клиент API университета
├── constants.py         # Константы
├── utils.py             # Утилиты
├── localizer.py         # Локализация
├── menu.py              # Меню и кнопки
├── requirements.txt     # Зависимости
└── README.md           # Документация
```

## Команды

- `/start` - Запустить бота
- `/l <логин> <пароль>` - Авторизация
- `/login` - Открыть меню выбора университета

## Деплой

### Docker

Создайте `Dockerfile`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "main.py"]
```

### Dockploy

1. Укажите `Dockerfile` в настройках сборки
2. В Environment Settings добавьте:
```
BOT_TOKEN=ваш_токен
STORAGE_TYPE=sqlite
SQLITE_PATH=/app/data/sessions.db
```

## Лицензия

MIT License
